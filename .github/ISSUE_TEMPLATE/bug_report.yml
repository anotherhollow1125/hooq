name: "🐛 Bug report"
description: "Something isn't working / 不具合報告"
title: "[Bug]: "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for reporting! Please fill out the following to help us reproduce and fix it.
        不具合報告ありがとうございます。再現と修正のため、以下を埋めてください。

  - type: textarea
    id: summary
    attributes:
      label: "📌 Summary / 概要"
      description: "What happened? / 何が起きましたか？"
      placeholder: |
        When I do X, Y happens (expected Z).
        XをするとYになりました（本来はZのはず）。
    validations:
      required: true

  - type: textarea
    id: repro
    attributes:
      label: "🔁 Steps to Reproduce / 再現手順"
      description: "Minimal steps or a minimal repository / 最小の手順または最小再現リポジトリ"
      placeholder: |
        1. ...
        2. ...
        3. ...
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: "✅ Expected Behavior / 期待する挙動"
      placeholder: "What you expected to happen / 本来どうなるべきか"
    validations:
      required: true

  - type: textarea
    id: actual
    attributes:
      label: "❌ Actual Behavior / 実際の挙動"
      placeholder: "What actually happened / 実際どうなったか"
    validations:
      required: true

  - type: textarea
    id: logs
    attributes:
      label: "📄 Logs / ログ"
      description: |
        Paste relevant logs, errors, panic/backtrace, etc.
        関連ログ（エラー、panic、backtrace等）を貼ってください。
      render: shell

  - type: textarea
    id: code
    attributes:
      label: "🧩 Minimal Code / 最小コード"
      description: |
        If possible, provide a minimal snippet.
        可能なら最小のコード断片を貼ってください。
      render: rust

  - type: textarea
    id: environment
    attributes:
      label: "🖥 Environment / 環境"
      description: "Versions and platform info / バージョン・環境情報"
      placeholder: |
        - hooq: x.y.z
        
        Following items are optional (以下は任意):
        - rustc: 1.xx (stable/nightly)
        - OS: (Windows/macOS/Linux)
        - Target: (x86_64-unknown-linux-gnu, wasm32-unknown-unknown, ...)
        - Features: (e.g. `default`, `tracing`, ...)
    validations:
      required: true

  - type: checkboxes
    id: checklist
    attributes:
      label: "✅ Checklist / チェックリスト"
      options:
        - label: "I searched existing issues and discussions. / 既存Issue/Discussionを検索しました"
          required: true
        - label: "I can reproduce this on the latest release (or main). / 最新版（またはmain）で再現します"
          required: false
        - label: "I included a minimal reproduction. / 最小再現を用意しました"
          required: false
        - label: "If this involves a security issue, I will not disclose details publicly. / セキュリティ関連なら詳細を公開しません"
          required: false

  - type: textarea
    id: additional
    attributes:
      label: "📝 Additional Context / 追加情報"
      description: "Anything else helpful / 補足があれば"
